# Personal website whose pages are added to the resume profile (optional).
PROFILE_WEBSITE=
# Optional search APIs (used by the "websearch" source). Leave blank to skip.
TAVILY_API_KEY=your-tavily-api-key
FIRECRAWL_API_KEY=your-firecrawl-api-key
